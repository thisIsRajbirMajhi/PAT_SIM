# Curated GUI preset: external-video input with virtual PTZ bypass.
preset_meta:
  preset: video_benchmark
  display_name: Video — Benchmark
  order: 40
  system: "deterministic"
  ai_mode: "OFF"
  purpose: Process an external MP4 through detection, tracking, metrics, and reports.
  expected:
    input: "External MP4"
    control: "Virtual PTZ bypassed"

experiment:
  seed: 53
  duration_s: 30.0
  input_mode: VIDEO
  video_path: data/input_videos/test_beacon.mp4
  preserve_native_fps: true
  expected_fps: 30
  resize_mode: preserve_reference_scale
  bypass_virtual_ptz: true

camera:
  control_mode: measurement_only
  video_centre_offset_x: 0.0
  video_centre_offset_y: 0.0

ai:
  enabled: false
  fallback_on_failure: true
  candidate_model_path: ""
  identity_model_path: ""

decoys:
  enabled: false
  count: 0
  profiles: []
