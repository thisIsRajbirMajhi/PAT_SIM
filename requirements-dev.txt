# Development dependencies for FSOC Tracker
# Install with: pip install -r requirements-dev.txt
-r requirements.txt
pytest>=7.0
pytest-qt>=4.0
pytest-cov>=4.0
black>=23.0
flake8>=6.0
mypy>=1.0
pre-commit>=3.0
matplotlib>=3.5  # for report plots (already required)
pyyaml>=6.0
